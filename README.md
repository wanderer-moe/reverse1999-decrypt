bundles decryptor for a certain game

make sure `main.py` and `/bundles` are in the same directory

## usage

```python
py main.py
```

the program will then put the decrypted file into `decrypted_bundles` folder
